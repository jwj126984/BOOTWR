# BMU 上位机通信协议

| 项目 | 说明 |
|------|------|
| 文档版本 | V3.1 |
| 适用硬件 | BMSLM_BMU_MD_V1.2 |
| 通信接口 | CAN 标准帧（250 kbps） |
| 用途 | 固件烧录、SN 读写、热电偶标定 |

---

## 1. 文档说明

上位机通过 CAN 与 BMU 通信，完成三类工作：

| 功能 | 说明 |
|------|------|
| **固件烧录** | 将 `.bin` 写入 Flash APP2 区并升级 |
| **SN 管理** | 读写设备序列号（最多 31 字符） |
| **热电偶标定** | 读写 k、b、偏置，读取采集电压用于标定 |

- 请求 ID：**`0x7E0`**，应答 ID：**`0x7E8`**
- 多字节整数、float：**大端（BE）**
- 应答 CMD = 请求 CMD **| `0x40`**（例：`0x10` → `0x50`）
- 应答 byte1 为**结果码**（`0x00` = 成功）

> **重要**：烧录与标定/SN **不能混状态**。标定、写 SN 时解锁后**不要发 `0x20`**；`0x16`/`0x12` 等只写 RAM，**必须再发 `0x13` 才会保存到 Flash**。

---

## 2. 命令总览（CMD 一览）

下表列出全部命令字，便于快速查阅；具体字节格式见后续各节。

| CMD | 名称 | 方向 | 简要说明 | 应答 CMD |
|-----|------|------|----------|----------|
| `0x10` | SESSION_ENTER | 主机→设备 | 解锁会话，密钥固定 **BMS1** | `0x50` |
| `0x11` | SESSION_EXIT | 主机→设备 | 退出会话 | `0x51` |
| `0x12` | TC_CALIB_WRITE | 主机→设备 | 写标定 k / b / vref 到 **RAM** | `0x52` |
| `0x13` | TC_CALIB_SAVE | 主机→设备 | **保存 RAM → Flash**（SN + 全部标定） | `0x53` |
| `0x14` | TC_CALIB_DEFAULTS | 主机→设备 | 恢复标定默认值到 **RAM** | `0x54` |
| `0x15` | TC_CALIB_READ | 主机→设备 | 读 RAM 中的 k / b / vref | `0x55` |
| `0x16` | SN_WRITE | 主机→设备 | 分段写 SN 到 **RAM** | `0x56` |
| `0x17` | SN_READ | 主机→设备 | 分段读 SN（可读 Flash 中已保存值） | `0x57` |
| `0x18` | TC_SAMPLE_READ | 主机→设备 | 读采集电压 **x** 或反解 **y** [V] | `0x58` |
| `0x20` | ERASE | 主机→设备 | 擦除 APP2，声明镜像大小与 CRC | `0x60` |
| `0x22` | VERIFY | 主机→设备 | 可选 CRC 校验（一般不必发） | `0x62` |
| `0x30` | COMMIT | 主机→设备 | 提交升级并**复位** | `0x70` |
| `0xF0` | GET_STATUS | 主机→设备 | 查询烧录状态/进度 | `0xD0` |
| `0x70`～`0x7F` | 数据帧 count | 主机→设备 | 烧录镜像数据（**无 CMD 字节**） | — |
| `0x65` | TRANSFER_DONE | **仅设备→主机** | 镜像收满且 CRC 通过后主动上报 | — |

### 2.1 按用途分类

| 用途 | 常用命令（顺序示例） |
|------|----------------------|
| **固件烧录** | `0x10` → `0x20` → 数据帧 → 等 `0x65` → `0x30` |
| **写 SN** | `0x10` → `0x16`（可多次）→ **`0x13` 保存** |
| **读 SN** | `0x10` → `0x17`（可多次） |
| **热电偶标定** | `0x10` → `0x12`/`0x15`/`0x18` → **`0x13` 保存** |

### 2.2 必须发 `0x13` 保存的情况（易忘）

下列命令**只改 RAM**，掉电即丢失，写完务必发 **`0x13` TC_CALIB_SAVE**：

| 操作 | 涉及命令 | 说明 |
|------|----------|------|
| 写 SN | `0x16` | 分段写完后发 `0x13`，SN 才写入 Flash |
| 写标定 | `0x12` | 写 k、b、vref 后发 `0x13` |
| 恢复默认标定 | `0x14` | 仅改 RAM，要持久化也需 `0x13` |

`0x13` 一次保存 **整包 NVM**（**SN + 全部热电偶标定**），不必为 SN 和标定各发一次。

### 2.3 结果码

| 值 | 含义 | 常见原因 |
|----|------|----------|
| `0x00` | 成功 | — |
| `0x02` | 密钥错 | `0x10` 密钥不是 BMS1 |
| `0x03` | 状态错 | 未解锁；标定时误发 `0x20` |
| `0x04` | 参数错 | 下标/长度非法等 |
| `0x05` | Flash 错 | 擦写失败 |
| `0x06` | CRC 错 | 镜像校验失败 |
| `0x07` | count 错 | 烧录数据帧序号不连续 |

---

## 3. 会话与公共命令（详细）

### 3.1 `0x10` SESSION_ENTER — 进入会话

| 字节 | 内容 |
|------|------|
| 0 | `0x10` |
| 1～4 | 密钥 u32 BE：`42 4D 53 31`（ASCII **BMS1**） |

- 应答 `0x50`，result=`0x00` 表示成功，进入 **SESSION** 状态
- 后续烧录、SN、标定均须先执行本命令

### 3.2 `0x11` SESSION_EXIT — 退出会话

- 请求：DLC=1，byte0=`0x11`
- 应答 `0x51`

### 3.3 `0xF0` GET_STATUS — 查询状态（烧录用）

- 请求：DLC=1，byte0=`0xF0`
- 应答 `0xD0`，byte2=state，byte3=last_err，byte4～6=已写字节数 u24 BE，byte7=下一期望 count

---

## 4. 固件烧录（详细）

### 4.1 目标区域

| 项目 | 值 |
|------|-----|
| 区域 | APP2 |
| 地址 | `0x08042000` |
| 容量 | 最大 **248 KB**（`0x3C000` 字节） |

### 4.2 推荐流程

```text
0x10 解锁 → 0x20 擦除 → 数据帧(0x70~0x7F) → 等待 0x65 → 0x30 复位
```

### 4.3 `0x20` ERASE

| 字节 | 内容 |
|------|------|
| 0 | `0x20` |
| 1～3 | `image_size` u24 BE（1～0x3C000） |
| 4～7 | `image_crc32` u32 BE |

- 擦除整个 APP2；应答 `0x60` 后进入 **RECEIVING**
- **下一帧数据帧 count 必须为 `0x70`**

### 4.4 数据帧（无 CMD，count `0x70`～`0x7F`）

| 字节 | 内容 |
|------|------|
| 0 | count，从 `0x70` 递增，`0x7F` 后回 `0x70` |
| 1～7 | 镜像数据，有效长 = DLC−1（1～7 字节） |

- count 须连续，否则 result=`0x07`
- 收满且 CRC 正确后设备**主动**发 `0x65`（一般不逐帧应答）
- 帧数约：`ceil(image_size / 7)`

### 4.5 `0x65` TRANSFER_DONE（设备应答）

| 字节 | 内容 |
|------|------|
| 0 | `0x65` |
| 1 | result |
| 2～4 | image_size u24 BE |
| 5 | 下一期望 count |

### 4.6 `0x30` COMMIT

| 字节 | 内容 |
|------|------|
| 0 | `0x30` |
| 1～4 | 魔数 `A5 5A A5 5A`（u32 BE） |

须 **READY** 状态；应答 `0x70` 后复位。

### 4.7 `0x22` VERIFY（可选）

一般不必发；收满后设备已自动 CRC。

### 4.8 烧录状态机

```text
IDLE --0x10--> SESSION --0x20--> RECEIVING --数据帧--> READY --0x30--> 复位
```

---

## 5. SN 读写（详细）

### 5.1 规则摘要

| 项目 | 说明 |
|------|------|
| 长度 | 最多 **31** 可见字符 + `\0` |
| 写 SN | `0x16` 分段写入 **RAM** |
| **持久化** | **写完 SN 后必须发 `0x13`**，否则掉电丢失 |
| 读 SN | `0x17` 读 RAM（已 `0x13` 保存后即为 Flash 内容） |
| 前提 | `0x10` 解锁，**勿发 `0x20`** |

### 5.2 推荐流程：写 SN

```text
0x10 解锁
  → 0x16 分段写（可发多帧，直至整串 SN 写完）
  → 0x13 保存到 Flash          ← 不可省略
  → 0x17 读回校验（可选）
  → 0x11 退出（可选）
```

### 5.3 `0x16` SN_WRITE

| 字节 | 内容 |
|------|------|
| 0 | `0x16` |
| 1 | offset：0～31 |
| 2 | len：1～5 |
| 3～ | SN 数据 |

应答 `0x56`。仅更新 RAM，**不会**自动写 Flash。

**示例**：写入 `BMU001`

```text
16 00 05  42 4D 55 30 30      // "BMU00"
16 05 02  31 00                // "1\0"
13                             // 保存（DLC=1）
```

### 5.4 `0x17` SN_READ

| 字节 | 内容 |
|------|------|
| 0 | `0x17` |
| 1 | offset |
| 2 | len：1～6 |

应答 `0x57`：byte2～7 为数据（最多 6 字节）。

---

## 6. 热电偶标定（详细）

### 6.1 电压关系（标定必读）

```text
  y（热电偶真实电压 [V]）──×51──► + vref_offset ──► x（GP4 采集电压 [V]）
```

公式：`x = y × 51 × k + b + vref_offset`（单位 **V**）

| 量 | 含义 | 来源 |
|----|------|------|
| **y** | 热电偶电动势 | 模拟器 / K 型表（**不要**加 vref、不要乘 51） |
| **x** | GP4 电压 | 设备采集，`0x18` sample_type=`0x01` |
| **k, b** | 通道修正 | 上位机标定后 `0x12` 写入 |
| **vref_offset** | 每片 AFE 偏置 | `0x12` param=`0x03`，每片 1 个 |

### 6.2 推荐流程

```text
0x10 解锁
  → 0x14 恢复默认（可选）
  → 0x12 写 3 片 vref
  → 多温度点：模拟器 y_ref + 0x18 读 x → 算 k、b
  → 0x12 写各通道 k、b
  → 0x13 保存到 Flash          ← 不可省略
  → 0x11 退出（可选）
```

两点法（vref 已固定）：  
`k = (x2−x1) / [51×(y2−y1)]`，`b = x1 − vref − 51×k×y1`

### 6.3 `0x12` TC_CALIB_WRITE

| 字节 | 内容 |
|------|------|
| 0 | `0x12` |
| 1 | param_type：`0x01`=k，`0x02`=b，`0x03`=vref |
| 2 | afe_idx：0～2 |
| 3 | tc_idx：0～7（vref 时忽略） |
| 4～7 | value float32 BE |

仅写 RAM；应答 `0x52`。

### 6.4 `0x15` TC_CALIB_READ

请求 DLC=4，前 4 字节同 `0x12`。应答 `0x55`，byte2～5 为 float32 BE。

### 6.5 `0x18` TC_SAMPLE_READ

| 字节 | 内容 |
|------|------|
| 0 | `0x18` |
| 1 | sample_type：`0x01`=**x** [V]；`0x02`=反解 **y** [V] |
| 2 | afe_idx |
| 3 | tc_idx |

应答 `0x58`，byte2～5 为 float32 BE。

- 读的是 **100ms 任务缓存**（与正常运行同路径），非即时重采
- 8 路轮询，读前等待该通道被扫到（最长约 **800ms**）
- 算 k、b 建议：**外部 y_ref + 0x18 读 x（0x01）**

### 6.6 `0x13` TC_CALIB_SAVE

| 字节 | 内容 |
|------|------|
| 0 | `0x13` |

将 RAM 中 **SN + 全部标定** 写入 Flash。应答 `0x53`。  
**写 SN（`0x16`）或写标定（`0x12`/`0x14`）后都必须执行本命令。**

### 6.7 `0x14` TC_CALIB_DEFAULTS

| 字节 | 内容 |
|------|------|
| 0 | `0x14` |

恢复 k=1、b=0、vref=0.1（仅 RAM）；要掉电保存须再 `0x13`。

---

## 7. 典型场景对照

| 场景 | 解锁后 | 结束前必做 |
|------|--------|------------|
| 只烧录 | `0x20` + 数据帧 | `0x30` 复位 |
| 只写 SN | `0x16`… | **`0x13` 保存** |
| 只标定 | `0x12`/`0x18`… | **`0x13` 保存** |
| SN + 标定 | 两者都写 RAM | **一次 `0x13`** 即可 |

```text
                    ┌── 0x20 + 数据帧 + 0x30     → 固件烧录
  0x10 解锁 SESSION ┤
                    └── 0x16/12/18… + 0x13      →  SN/标定（勿发 0x20）
```

---

## 8. 修订记录

| 版本 | 说明 |
|------|------|
| V3.1 | 章首增加 CMD 总览；强调 SN/标定写 RAM 后须 `0x13` 保存 |
| V3.0 | 更名为《上位机通信协议》；结构重组 |
| V2.3 | 新增 `0x18` 读采集 x/y |
| V2.2 | 标定 `0x12`～`0x15`、SN `0x16`/`0x17` |
| V2.0 | 流式烧录 + 自动 CRC |

---

## 9. 相关文档

- [`CAN烧录说明.md`](CAN烧录说明.md)
- [`CAN烧录上位机指南.md`](CAN烧录上位机指南.md)
