# BMU CAN 烧录上位机指南（报文示例）

| 版本 | V2.0 |
| 协议 | `docs/CAN烧录通信协议.md` |

---

## 1. 约定

| 项 | 值 |
|----|-----|
| ID | 发 `0x7E0`，收 `0x7E8` |
| DLC | 通常 8；末帧可更短 |
| 整数 | **大端** |
| 下载数据帧 | **无 CMD**，`[count][最多 7 字节数据]` |

---

## 2. 数据帧格式（核心）

```text
┌────────┬──────────────────────────────────────┐
│ count  │  D0   D1   D2   D3   D4   D5   D6    │  DLC = 1 + 有效数据长度
│ 1 字节 │           最多 7 字节                  │
└────────┴──────────────────────────────────────┘
```

| 规则 | 说明 |
|------|------|
| count 初值 | 收到 **ERASE 应答 `0x60 00`** 后为 **0** |
| count 步进 | 每发一帧 **+1**，`255` 下一帧为 **0**（`& 0xFF`） |
| 设备应答 | **不发** 0x21/0x24；收满 `image_size` 后 **一条 `0x65`** |
| 帧数 | `N = (文件字节数 + 6) // 7` |

**示例**（68 字节文件，10 帧）：

| 帧 | count | DATA[0]…[7]（十六进制） |
|----|-------|-------------------------|
| 1 | 0 | `00 00 01 02 03 04 05 06` |
| 2 | 1 | `01 07 08 09 0A 0B 0C 0D` |
| … | … | … |
| 10 | 9 | `09 3C 3D 3E 3F 40 41 42 43`（仅 5 字节有效时 DLC=6 亦可） |

---

## 3. 完整流程报文

### 3.1 SESSION — `0x10`

| 方向 | DATA[0]…[7] |
|------|-------------|
| 主机→ | `10 42 4D 53 31 00 00 00` |
| 设备→ | `50 00 00 00 00 00 00 00` |

### 3.2 ERASE — `0x20`

格式：`[20][size u24 BE][crc32 u32 BE]`

68 字节、`crc` 占位 `A1 B2 C3 D4`：

| 方向 | DATA[0]…[7] |
|------|-------------|
| 主机→ | `20 00 00 44 A1 B2 C3 D4` |
| 设备→ | `60 00 00 00 00 00 00 00` |

**此后立即连续发数据帧，无需等块应答。**

### 3.3 数据流

对 BIN 逐段取 7 字节，按 §2 组帧，`count` 从 0 递增。

### 3.4 收满应答 — `0x65`

| 方向 | DATA[0]…[7] |
|------|-------------|
| 设备→ | `65 00 00 00 44 [下一count] 00 00` |

- `65` = `0x25 | 0x40`
- `00 00 44` = 确认 size=68
- 失败：`65 06 …`（CRC）、`65 07 …`（count）

**主机在发完最后一帧后等待 `0x65`，超时建议 30 s。**

### 3.5 COMMIT — `0x30`

| 方向 | DATA[0]…[7] |
|------|-------------|
| 主机→ | `30 A5 5A A5 5A 00 00 00` |
| 设备→ | `70 00 00 00 00 00 00 00` |

---

## 4. 打包示例（Python）

```python
PAYLOAD = 7
cnt = 0
off = 0
while off < len(img):
    chunk = img[off : off + PAYLOAD]
    bus.send({"arbitration_id": 0x7E0,
              "data": bytes([cnt & 0xFF]) + chunk.ljust(8 - 1, b"\x00")[:7],
              "is_extended_id": False})
    cnt = (cnt + 1) & 0xFF
    off += PAYLOAD

# 等待 0x65
```

或使用 `tools/can_prog_host.py`。

---

## 5. 与旧版区别

| 旧版 (V1) | 新版 (V2) |
|-----------|-----------|
| 0x21/0x23/0x24 分块 | 仅 `[count][数据]` |
| count u16 大端 | count **u8**，0~255 循环 |
| 每块等 0x24 | **收满一次 0x65** |
| 需发 0x22 VERIFY | **自动校验**（0x22 可选） |

---

## 6. 修订记录

| 版本 | 说明 |
|------|------|
| V2.0 | 流式 count+7B 协议 |
| V1.x | 分块协议（已废弃） |
